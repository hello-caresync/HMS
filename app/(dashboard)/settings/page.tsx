import SystemSettingsWorkbench from '../../testing/settings/components/SystemSettingsWorkbench';

export const metadata = {
  title: 'System Settings · CuraSync ERP',
  description: 'High-contrast global configuration terminal for sandbox testing',
};

export default function SettingsPage() {
  return <SystemSettingsWorkbench />;
}
