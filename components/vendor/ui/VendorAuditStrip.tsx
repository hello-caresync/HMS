'use client'; type VendorAuditStripProps = { entries: { id: string; actor: string; action: string; resource: string; timestamp: string }[];
}; export function VendorAuditStrip({ entries }: VendorAuditStripProps) { return ( <section className="rounded-2xl border border-dashed border-slate-300 bg-slate-50/80 p-4 bg-vendor-card/40" aria-label="Recent audit events" > <p className="text-[10px] font-black uppercase tracking-wider text-vendor-muted">Audit trail (immutable)</p> <ul className="mt-2 space-y-1.5"> {entries.map((e) => ( <li key={e.id} className="flex flex-wrap gap-x-2 text-xs text-vendor-muted text-vendor-muted"> <span className="font-bold text-slate-800 text-vendor-charcoal">{e.timestamp}</span> <span>{e.actor}</span> <span>· {e.action}</span> <span className="font-mono">{e.resource}</span> </li> ))} </ul> </section> );
}
