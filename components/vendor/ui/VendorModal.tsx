'use client'; import type { ReactNode } from 'react'; import { vendorClasses } from '@/lib/vendor/theme'; type VendorModalProps = { title: string; open: boolean; onClose: () => void; children: ReactNode; footer?: ReactNode;
}; export function VendorModal({ title, open, onClose, children, footer }: VendorModalProps) { if (!open) return null; return ( <div className="fixed inset-0 z-50 flex items-center justify-center p-4" role="dialog" aria-modal="true" aria-labelledby="vendor-modal-title" > <button type="button" className="absolute inset-0 bg-vendor-charcoal/55" aria-label="Close dialog" onClick={onClose} /> <div className={`relative z-10 w-full max-w-lg p-6 shadow-xl ${vendorClasses.card}`}> <div className="flex items-start justify-between gap-4"> <h2 id="vendor-modal-title" className="text-lg font-black text-vendor-charcoal"> {title} </h2> <button type="button" onClick={onClose} className="rounded-lg px-2 py-1 text-sm font-bold text-vendor-muted hover:bg-vendor-cream" > ✕ </button> </div> <div className="mt-4">{children}</div> {footer ? <div className="mt-6 flex flex-wrap justify-end gap-2">{footer}</div> : null} </div> </div> );
} export { vendorFieldClass, vendorLabelClass } from '@/lib/vendor/ui-tokens';
