'use client'; import type { LucideIcon } from 'lucide-react'; import { vendorClasses } from '@/lib/vendor/theme'; type VendorStatCardProps = { label: string; value: string | number; hint?: string; icon: LucideIcon; tone?: 'default' | 'warning' | 'success';
}; export function VendorStatCard({ label, value, hint, icon: Icon, tone = 'default' }: VendorStatCardProps) { const toneClass = tone === 'warning' ? 'border-vendor-warning/35 bg-vendor-warning/10' : tone === 'success' ? 'border-vendor-success/35 bg-vendor-success/10' : 'border-vendor-accent/20 bg-vendor-card'; return ( <div className={`rounded-2xl border p-4 shadow-sm ${toneClass}`}> <div className="flex items-start justify-between gap-2"> <p className="text-[10px] font-bold uppercase tracking-wider text-vendor-muted">{label}</p> <Icon className="h-4 w-4 shrink-0 text-vendor-secondary" aria-hidden /> </div> <p className="mt-2 text-2xl font-black tabular-nums text-vendor-charcoal">{value}</p> {hint ? <p className="mt-1 text-xs font-medium text-vendor-muted">{hint}</p> : null} </div> );
}
