'use client';

import { BusinessDashboard } from '@/components/vendor/workspaces/BusinessDashboard';

/** Nexora Vendor · business dashboard (Supabase KPIs + demo fallbacks). */
function DashboardWorkspace() {
  return <BusinessDashboard />;
}

export default DashboardWorkspace;
export { DashboardWorkspace };
