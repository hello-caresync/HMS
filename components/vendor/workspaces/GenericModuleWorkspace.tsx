'use client'; import { VendorModuleHeader } from '@/components/vendor/ui/VendorModuleHeader';
import type { VendorModuleConfig } from '@/lib/vendor/modules/registry';
import { useActiveHospital } from '@/lib/vendor/store/vendor-app-store'; type GenericModuleWorkspaceProps = { config: VendorModuleConfig;
}; export function GenericModuleWorkspace({ config }: GenericModuleWorkspaceProps) { const hospital = useActiveHospital(); return ( <div className="space-y-6"> <VendorModuleHeader title={config.title} description={`${config.description} · Context: ${hospital.name}.`} actions={ <button type="button" className="rounded-xl border border-vendor-accent/20 bg-vendor-card px-4 py-2 text-xs font-bold text-slate-800 shadow-sm hover:bg-slate-50 bg-vendor-card text-vendor-charcoal" > Export audit log </button> } /> <ul className="flex flex-wrap gap-2"> {config.features.map((feature) => ( <li key={feature} className="rounded-full border border-vendor-accent/40 bg-vendor-primary/15 px-3 py-1 text-[10px] font-bold uppercase tracking-wide text-vendor-secondary " > {feature} </li> ))} </ul> <section className="rounded-2xl border border-vendor-accent/20 bg-vendor-card p-6 shadow-sm border-vendor-accent/20 bg-vendor-card"> <h2 className="text-sm font-black text-vendor-charcoal text-vendor-charcoal">{config.panelTitle}</h2> <div className="mt-4">{config.panelBody}</div> </section> <section className="rounded-2xl border border-dashed border-slate-300 bg-slate-50/80 p-6 text-sm text-vendor-muted bg-vendor-card/50 text-vendor-muted"> <p className="font-bold text-slate-800 text-vendor-charcoal">EDI / API integration hooks</p> <p className="mt-2"> Connect enterprise ERP (SAP, Oracle, Tally) via webhooks and AS2/EDI placeholders. All actions are recorded in immutable audit logs with masked IP and actor RBAC scope. </p> </section> </div> );
}
