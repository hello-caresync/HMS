generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// PASTE YOUR TABLES HERE (doctors, patients, encounters, prescriptions, etc.)